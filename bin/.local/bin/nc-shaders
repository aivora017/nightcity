#!/usr/bin/env bash
# nc-shaders — compile every .frag in the quickshell shaders folder to .qsb
set -e
cd "$HOME/.config/quickshell/shaders"
for f in *.frag; do
  /usr/lib/qt6/bin/qsb --glsl "100 es,120,150" --hlsl 50 --msl 12 -o "$f.qsb" "$f"
  echo "compiled $f"
done
